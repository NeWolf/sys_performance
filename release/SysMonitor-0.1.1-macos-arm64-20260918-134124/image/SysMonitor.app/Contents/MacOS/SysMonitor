#!/bin/sh
# Keep the server visible in Terminal; Control-C exits without touching Android.
set -eu
HERE=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec /usr/bin/open -a Terminal "$HERE/../Resources/Start.command"